[project]
name = "akretic-a2a-trust-gateway"
version = "0.1.0"
description = "Akretic A2A Trust Gateway challenge prototype starter"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"

[tool.ruff]
line-length = 100
